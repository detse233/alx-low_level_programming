#include <stdio.h>
/**
 * main - print name then new line
 *
 * @argc: counts number of arguments
 * @argv: array that contain arguments
 *
 * Return: (0)
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
